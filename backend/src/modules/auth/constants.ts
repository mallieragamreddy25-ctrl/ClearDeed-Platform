/**
 * Authentication Module Constants
 */

export const OTP_EXPIRY = 600; // 10 minutes in seconds
export const MAX_OTP_ATTEMPTS = 5; // Maximum OTP verification attempts
export const JWT_EXPIRY = '24h'; // JWT token expiry
export const OTP_LENGTH = 6; // OTP digit length
export const RATE_LIMIT_WINDOW = 600; // Rate limit window in seconds (10 minutes)
